To deploy this Flask App, run the following commands:

For Windows's Powershell:

```
$env:FLASK_APP = "predict"
flask run --host=0.0.0.0
```

For Linux's Bash:

```
export FLASK_APP=predict
flask run --host=0.0.0.0
```
